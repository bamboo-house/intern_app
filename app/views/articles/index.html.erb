<div class="main">
  <%# event_titleでまとめて表示 %>
  <ul>
    <%@datas.each do |d|%>
      <li class="items">
        <p class="group-name "><%= d.group_name%></p>
        <%=link_to d.event_title, d.link_url, class: "info-event"%>
      </li>
    <%end%>
  </ul>
  <%= paginate @datas%> 
</div>